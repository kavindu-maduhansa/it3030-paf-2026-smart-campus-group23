# Browser opens this origin for OAuth redirects (must match Spring Boot port).
# Leave empty to use Vite /api + /ws proxy (recommended in dev for session cookies).
VITE_API_URL=
VITE_BACKEND_ORIGIN=http://127.0.0.1:8080
