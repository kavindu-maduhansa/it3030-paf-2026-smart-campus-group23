# Browser opens this origin for OAuth redirects (must match Spring Boot port).
# Keep API URL empty in dev to use Vite /api + /ws proxy and avoid cross-port cookie mismatch.
VITE_BACKEND_ORIGIN=http://localhost:8081
VITE_API_URL=